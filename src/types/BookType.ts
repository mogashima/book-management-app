export interface Book {
    _id?: string
    title: string
    author: string
    publisher: string
    isbn: string
    price: number
    description: string
    memo: string
    purchasedAt: string
    borrowedAt: string
}